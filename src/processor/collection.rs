use std::fs;
use std::fs::File;
use std::io::Read;
use std::path::{Path, PathBuf};

use crate::motor;

// TODO

pub fn gen_context() {
    // return [];
}
pub fn gen_item(
    // context: Context,
    template_path: PathBuf,
    input_path: PathBuf,
    output_path: PathBuf,
    language: Option<&String>,
) {
}
